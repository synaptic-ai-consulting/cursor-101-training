# TaskFlow API — Cursor Rules
# ============================================================
# SESSION 1 LAB: This file is intentionally sparse.
# Your task is to expand these rules to encode the full
# architecture of this project. Use @codebase to explore
# the patterns already in use, then write rules that enforce them.
# ============================================================

## Project Overview
This is a Node.js + TypeScript REST API for task management.
Stack: Express, PostgreSQL (via node-postgres), Jest, TypeScript strict mode.

## What to add in Session 1 Lab:
# - Naming conventions (routes, services, DTOs)
# - Error handling standards (always use AppError, always pass to next())
# - Response format (always use ApiResponse<T> wrapper)
# - Validation rules (all POST/PATCH routes must apply validate middleware)
# - Test conventions (describe/it structure, supertest for integration tests)
# - Banned patterns (no raw try/catch without next(err), no console.log in production)
